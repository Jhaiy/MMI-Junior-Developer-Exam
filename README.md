# Pokémon OCR Project

A Pokémon card OCR system that processes different parts of a Pokémon card using separate OCR services and stores the processed data in a Supabase database.

## Running the Project

You can run each OCR service separately if you prefer.

Alternatively, you can run all the services at the same time using Docker Compose:

```bash
docker compose up --build
```
